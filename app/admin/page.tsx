import { FC } from "react";

interface pageProps {}

const page: FC<pageProps> = ({}) => {
  return (
    <div className="flex justify-between  items-center">
      <div></div>
    </div>
  );
};

export default page;
